# Build Roadmap

1. Create Firebase project (free), enable Firestore, get web config keys
2. Build static app: `index.html` + `app.js` + `style.css`
   - Rankings board (modern card layout, photos, rank #, W-L, points)
   - "Log a game" form: pick two players, enter scores → submit
   - Player history view (tap a player → their game log)
   - Live updates via Firestore listeners (everyone sees changes instantly, no refresh)
3. Add `/photos` folder with placeholder images
4. Push to GitHub repo, enable GitHub Pages
5. Share the Pages URL with Big 7 group chat

Say the word and I'll start building — first the Firebase setup steps, then the app.
